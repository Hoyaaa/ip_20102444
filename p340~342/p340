10번 문제: 자바스크립트에서 함수 코드 감싸는 기호
질문: 자바스크립트에서 함수를 만들 때 함수 코드를 감싸는 데 사용되는 기호는 무엇인가요?
보기:
① []
② {}
③ ()
④ <>

답:② {}

function 함수이름() {
    // 함수의 내용 (코드)
}


11번 문제: 코드 실행 결과 예측
(1) 코드
alert(10 + 20 + '!!!!');

자바스크립트에서 + 연산자는 숫자와 문자열이 함께 사용될 경우 문자열 결합으로 동작
30 + '!!!!!' → 숫자와 문자열을 더하면 문자열로 결합되어 "30!!!!"

(2) 코드
//변수를 선언합니다.
let number = 10;

// 출력합니다
alert(+number); // 첫 번째 alert
alert(++number); // 두 번째 alert
alert(+number); // 세 번째 alert

첫 번째 줄:
+number는 number를 숫자로 변환,number가 이미 숫자이므로 값은 10
두 번째 줄:
++number는 전위 증가 연산자로, number의 값을 1 증가시킨 뒤 반환, number는 11이 되고, alert는 11을 출력
세 번째 줄:
마지막으로 +number는 현재 값인 11을 그대로 출력

10
11
11

(3) 코드
let test = function () { return 10 }
function test() { return 20 }

//출력합니다
alert(test)

동일한 이름의 함수와 변수가 선언되었을 경우, 변수 선언이 함수 선언을 덮음
따라서 let test = function () { return 10; }는 변수 test에 익명 함수를 할당한 상태로 적용
이후 alert(test)는 test 변수에 저장된 함수 자체를 출력하며, 이는 함수의 코드인 function () { return 10 }

12번 문제: =와 ==의 차이
=: 대입 연산자, 변수에 값을 할당하는 데 사용
==: 동등 비교 연산자, 값이 같으면 true를 반환하지만, 자료형을 변환하여 비교

13번 문제: if 조건문 논리 연산자로 변경
if (x > 10) {
    if (x < 20) {
        alert(x);
    }
}

답:
if (x > 10 && x < 20) {
    alert(x);
}

주어진 코드는 두 개의 if 조건을 중첩하여 x > 10과 x < 20을 모두 만족할 경우에만 alert(x)를 실행
이를 **논리 연산자 &&**를 사용하여 하나의 if 문으로 통합하면 동일한 동작을 구현
논리 연산자 &&는 두 조건이 모두 true일 때만 전체 조건을 true
